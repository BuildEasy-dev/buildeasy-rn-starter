# Personal Development Configuration Template
# Copy this file to .env.local for personal development overrides
# .env.local is NOT committed to git
# Note: These values are ONLY for local development - NOT used in EAS builds

# Examples of personal overrides:

# Use your local IP for device testing
# EXPO_PUBLIC_API_URL=http://192.168.1.100:3000

# Enable analytics for local testing
# EXPO_PUBLIC_ANALYTICS_ENABLED=true

# Disable debug logging
# EXPO_PUBLIC_DEBUG_LOGGING_ENABLED=false

# EAS Secrets (Set via: eas secret:create --scope project --name SECRET_NAME --value secret_value)
# These should NOT be set in .env files, only as examples

# iOS App Store Connect (for automatic submission)
# EXPO_APPLE_ID=your-apple-id@example.com
# EXPO_ASC_APP_ID=your-app-store-connect-app-id
# EXPO_APPLE_TEAM_ID=your-apple-team-id

# Android Google Play (for automatic submission)
# EXPO_ANDROID_SERVICE_ACCOUNT_KEY_PATH=path/to/service-account-key.json